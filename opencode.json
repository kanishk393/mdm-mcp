{
  "$schema": "https://opencode.ai/config.json",
  "mcp": {
    "master-data": {
      "type": "local",
      "command": ["docker", "run", "-i", "--rm", "-v", "mdm-data:/data", "mdm-mcp:latest"],
      "enabled": true
    }
  }
}
